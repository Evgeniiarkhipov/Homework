# -------------------------------------Задание № 1-------------------------------------------
"""
Найдите количество различных элементов данного массива.
"""
matrix = [[1, 2, 4],
          [4, 5, 6],
          [6, 7, 8]]
list_ = []
for row in matrix:
    for el in row:
        list_.append(el)
print(list_) # проверка сгенерированного списка
print(f"кол-во различных элементов исходного массива = {len(set(list_))}")
# -------------------------------------------------------------------------------------------
# -------------------------------------Задание № 2-------------------------------------------
"""
Найти наиболее часто встречающийся элемент в массиве целых чисел.
"""
matrix = [[1, 2, 5],
          [2, 4, 4],
          [6, 0, 4]]
# Генерируем список из массива
list_ = []
for row in matrix:
    for el in row:
        list_.append(el)
print(list_) # проверка сгенерированного списка
# Ищем наиболее встречающийся элемент
for i in range(len(list_) - 1):
    if list_.count(list_[i]) > list_.count(list_[i + 1]):
        count_el = list_.count(list_[i])
        el = list_[i]
print(f"Наиболее встречающийся элемент - {el}, встречается {count_el} раза")
# -------------------------------------------------------------------------------------------
# -------------------------------------Задание № 3-------------------------------------------
"""
В данном массиве найти два наименьших элемента.
"""
matrix = [[1, 2, 5],
          [2, 4, 4],
          [6, 1, 1]]
# Генерируем список из массива
list_ = []
for row in matrix:
    for el in row:
        list_.append(el)
print(list_) # проверка сгенерированного списка
list_.sort()
print(f"Первый наименьший элемент - {list_[0]}, второй наименьший элемент - {list_[1]}")
# -------------------------------------------------------------------------------------------
# -------------------------------------Задание № 4-------------------------------------------
"""
напечатать прямоугольник (заданы длина, ширина и символ) при помощи цикла for
"""
a = 5
b = 6
symbol = '*'
if a > 0 and b > 0:
    for i in range(b):
        print(symbol * a)
else: print("Невозможно нарисовать прямоугольник")
# -------------------------------------------------------------------------------------------
# -------------------------------------Задание № 5-------------------------------------------
